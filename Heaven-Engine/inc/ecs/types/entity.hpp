#pragma once

#ifndef ENTITY_HPP
#define ENTITY_HPP

// include standard library
#include <cstdint>

// type alias to define a entity
using Entity = std::uint32_t;

// define the maximum amount of entites
const Entity MAX_ENTITIES = 1000000;

#endif